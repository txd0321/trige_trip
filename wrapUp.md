# 2026-01-25 工作总结

## 今日总体进度
- **登录体系**：完成从 UI → 逻辑 → 跳转的完整重构。
- **核心解锁闭环（周一目标）**：后端数据结构、云函数、示例数据全部打通，并在详情页验证成功。

---
## 关键改动 & 产出

| 模块 | 产出文件 / 集合 | 关键字段/接口 | 说明 |
| ---- | --------------- | -------------- | ---- |
| 登录页 | `pages/login/*` | `userInfo.avatarUrl`, `userInfo.nickName`, `avatarReady`, `nameReady` | 头像 + 昵称分别授权；`完成` 按钮触发 `login` → `updateUser` → Loading 页；样式内联 `templet.wxss` 并修复头像正圆问题。 |
| Loading 页 | `pages/loading/*` | — | 欢迎文案 1.5 s 后 `switchTab` 首页。 |
| 卡牌集合 | `cards` | `_id`, `name`, `cupType`, `cover` | 导入 4 条示例卡牌（JSON Lines）。 |
| 用户卡牌集合 | `user_cards` | `_openid`, `cardId`, `unlockedAt` | 记录用户解锁状态。 |
| 云函数 | `unlockCard` | 入参 `cupId`；返回 `{ success, repeat, cardInfo }` | cupId→card 查询，写 `user_cards`。部署后日志验证成功。 |
| 详情页 | `pages/card/detail/*` | URL 参数 `cupId` | 调 `unlockCard`，根据 `repeat` 显示“解锁成功 / 已解锁”。 |
| 路由 | `app.json` | 新增页面顺序 | 注册 `loading`、`card/detail` 页面。 |

---
## 今日遇到的问题 & 解决方案

1. **头像显示为椭圆 / 被截断**
   - *原因*：`button` 系统样式干扰尺寸。
   - *解决*：使用 `circle-wrapper` 固定尺寸 + 透明 `button` 覆盖；`overflow:hidden` 保证正圆。

2. **导入 cards.json 报 JSON Lines 格式错误**
   - *原因*：初始文件为普通 JSON 数组。
   - *解决*：改为 `.jsonl` 一行一条记录或直接复制粘贴批量导入。

3. **unlockCard 返回 event 而非业务数据**
   - *原因*：云函数入口未部署正确代码（仍为默认模板）。
   - *解决*：确认 `unlockCard/index.js` 路径及入口 `index.main`，重新部署云端安装依赖。

4. **详情页缺少 cupId**
   - *原因*：页面路径参数拼写为 `cupid`。
   - *解决*：修正为 `/pages/card/detail?cupId=CUP_001`。

5. **头像灰色占位一直显示**
   - *原因*：`thirdwx.qlogo.cn` 未加入 downloadFile 合法域名。
   - *解决*：在公众平台 `服务器域名 → downloadFile` 添加 `thirdwx.qlogo.cn`, `wx.qlogo.cn`。

---
## 下一步计划（W2-5）
- 批量生成 URL Scheme：`pages/card/detail?cupId=xxx`。
- 制作二维码并线下测试微信扫一扫直达详情页。

> 所有代码已提交至本地 Git；推送远程时若遇 400 错误，可检查 URL、代理、2FA Token 等。

---
## 2026-01-26 AR 探索页迭代总结

- **核心决策**：由于个人主体无法使用 OpenCV/TFLite 插件实现高精度实时识别，且轻量 JS 投影法在 8mm 小图案 + 手抖场景下稳定性不足，**决定暂停当前 AR 识别算法优化**，待小程序主体升级为**企业主体**后，再用 **OpenCV** 或 **TensorFlow Lite** 插件重构该功能。

- **AR 页面 MVP** (`pages/ar/index`):
  - **UI**：实现了全屏摄像头、居中竖向 ROI 框（100*700rpx）、5 段分割线、顶部状态文本和底部进度条。
  - **识别逻辑探索**（已写入代码，但效果不佳）：
    1. **投影峰值法**：通过横向投影曲线的峰值数量区分 `●` 和 `❌`。
    2. **3×3 网格密度 + 模板匹配**：用 9 维向量与预设模板做余弦相似度，并加入黑量、置信差距、锁定机制等抗干扰策略。
  - **任务序列**：实现了按 13 步固定序列（`[4,4,3,...]`）匹配圆位置来推进任务的逻辑。

- **遇到的问题 & 解决方案**:
  1. **白屏/无摄像头画面**：通过强制 `position:fixed`、`z-index` 分层、`canvas` 尺寸对齐屏幕、确保相机权限等方式修复。
  2. **ROI/识别框不可见**：通过 `canvas` 尺寸对齐、移除 `type=2d`、CSS `z-index` 修正等方式解决。
  3. **识别乱跳/误判**：尝试了 EMA 平滑、中位数阈值、连续帧确认、冷却节流、置信度差距、锁定机制等多种策略，但受限于 JS 算法本身，对小尺寸+手抖场景仍不够鲁棒。
- **下一步**：
  - **搁置 AR 识别**：等待主体升级。
  - **转向其他模块**：可继续开发“发条解锁与旋转页”（烧3页面F）、“我的”页卡牌列表、AI 聊天等不受主体限制的功能。


---
## 2026-01-30  杯子精灵主页迭代总结（3D → 视频版）

### 目标
将首页改为 **全屏视频精灵** + 占位图方案，并解决包体、黑屏、声音等体验问题。

### 关键里程碑
| 时间 | 成果 | 关键文件 |
|------|------|-----------|
| 1) PRD 对齐 | • 完成 `prd.md`「页面H 杯子精灵主页」补全：用户故事/功能点/前端元素/设计风格/主题色 | `prd.md` |
| 2) 首页从 3D 改为视频 | • 放弃 Three.js 实时渲染，改用 `video` 播放 mp4<br>• 支持点击切换动作 | `pages/home/*` |
| 3) 主页卡牌迁移到“我的” | • 移除首页卡牌列表<br>• 在 `pages/profile` 加入卡牌列表、分页加载、封面兜底 | `pages/home/index.wxml` `pages/profile/*` |
| 4) 包体限制处理 | • 真机调试触发 2MB 限制（主包/分包均受限）<br>• 资源迁移至 COS，避免打包超限 | COS 控制台 + `pages/home/index.js` |
| 5) COS/CDN 配置与排障 | • 指导创建 COS 桶（广州地域）、权限、公有读、告警/加密/标签等设置选择<br>• 处理 `catlook.mp4` 命名错误导致 404 | COS + `pages/home/index.js` |
| 6) 全屏与控件 | • 视频全屏 `object-fit: cover`<br>• 去掉默认控件条（`controls=false`）<br>• `AR 探索` 用 `catchtap` 防止冒泡 | `pages/home/index.wxml/.wxss` |
| 7) 黑屏彻底消除 | • 用 `catstop.png` 作为占位层<br>• 切换时机改为：先显示占位→等待 `loadedmetadata/play`→再隐藏占位 | `pages/home/index.js` |
| 8) 最终播放策略 | • Idle：常显 `catstop`，每 10 秒播放一次 `catlook`<br>• 点击：随机播放 `catdance/catspeak/catyawn` 一次 | `pages/home/index.js` |
| 9) 声音播放 | • 直接让视频自带音：移除 `muted` 属性（避免额外 mp3 管理） | `pages/home/index.wxml` |

### 当前主页交互逻辑（对齐 PRD）
- **默认状态**：展示占位图 `catstop.png`。
- **自动播放**：每 10 秒自动播放一次 `catlook.mp4`（约 5 秒），播放开始后隐藏占位图。
- **点击触发**：点击屏幕随机播放 `catdance/catspeak/catyawn` 其一（约 5 秒），结束后回到占位图并恢复自动播放。
- **跳转**：点击 `AR 探索` 跳转 `/pages/ar/index`。

### 风险与待办
1. **iOS 自动播放策略**：首段自动播放可能被静音/拦截；目前点击触发播放不受影响。
2. **资源命名规范**：mp4/png 文件名需与代码常量完全一致（大小写敏感）。
3. **成本监控**：资源走 COS/CDN 会产生下行流量，建议关注告警与用量面板。

02/10
- 修复云开发环境 ID 配置，打通云函数与数据库 (`users`, `user_cards`, `cards`)。
- 根据 Figma 重构 Login 页面，支持微信原生头像选择与昵称快捷填充。
- 重构 Profile 页面布局，实现卡牌网格与勋章墙，并完成 16px 边距的精准对齐。
- 实现 Profile 页面“未获得”状态的空占位逻辑。
- 新增“关于我们”与“意见反馈”功能页面，完成路由注册与跳转逻辑闭环。
- 优化反馈页面 UI，调整卡片边距、按钮布局及描述文字间距。

02/13
- 实现“扫码 -> 待解锁详情 -> 杯子识别 -> 自动解锁并返回”完整闭环逻辑。
- 详情页支持普通链接 (q参数) 与小程序码 (scene参数) 解析，实现扫码即达。
- 新建并部署 getQRCode 云函数，支持一键生成特定卡牌的小程序码并存入云存储。
- Profile 页面重构：用户信息区整体居中，修复昵称对齐偏差，重构横向卡牌及 100px 虚线占位区。
- 视觉优化：统一全页面标题字号、颜色 (#666666) 与常规粗细，全页 16px 边距精准对齐。
- 性能优化：加固 TabBar 同步逻辑与生命周期清理，增加视频报错降级保护，精简编译配置解决模拟器死锁。
- 详情页 UI 优化：锁图标移至上方，状态标志精准对齐，按钮固定距离底部 64px。
